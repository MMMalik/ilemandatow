export interface Theme {
  primary: string;
  bgPrimary: string;
  bgSecondary: string;
  textPrimary: string;
  textOnPrimary: string;
  textMuted: string;
  textDisabled: string;
  defs: { [key: string]: string };
}
