import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import Tooltip from "./Tooltip";
import * as template from "./Tooltip.stories";

<Meta title="Data Display/Tooltip" component={Tooltip} />

### Tooltip

Default tooltip component. It's using `Popper` under the hood along with custom css helpers.

<Canvas>
  <Story
    name="Main"
    args={{ children: <div>{"Hover here!"}</div>, title: "Tootlip text" }}
  >
    {template.Tooltip.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable of={Tooltip} />
