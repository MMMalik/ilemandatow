import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import Form from "./Form";
import * as template from "./Form.stories";

<Meta title="Form/Form" component={Form} />

### Form

Root form component. Use it with other form components. Package `react-hooks-form` is used under the hood.

<Canvas>
  <Story name="Main">{template.Form.bind({})}</Story>
</Canvas>

### Props

<ArgsTable of={Form} />
