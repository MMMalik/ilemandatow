import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import Button from "../button";
import DismissablePopper from "./DismissablePopper";
import * as template from "./DismissablePopper.stories";

<Meta title="Feedback/DismissablePopper" component={DismissablePopper} />

### DismissablePopper

This component serves as a base for menu-like components such as `Menu`. It's a wrapper around `Popper` component which makes poppers disappear upon clicking.

<Canvas>
  <Story
    name="Main"
    args={{
      children: <div className="pa3">{"Dismissiable popper's content"}</div>,
      clickableComponent: (onClick) => (
        <Button onClick={onClick}>{"Click me"}</Button>
      ),
    }}
  >
    {template.DismissablePopper.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable of={DismissablePopper} />
