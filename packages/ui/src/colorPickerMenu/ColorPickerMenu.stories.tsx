import * as React from "react";
import ColorPickerMenuCmp, { ColorPickerMenuProps } from "./ColorPickerMenu";

export const ColorPickerMenu = (args: ColorPickerMenuProps) => {
  return <ColorPickerMenuCmp {...args} />;
};
