export interface ChartParty {
  /**
   * Fill color of party's seats.
   */
  fill: string;
  /**
   * Number of party's seats.
   */
  seats: number;
  /**
   * Party's unique id.
   */
  id: string;
  /**
   * Party's label.
   */
  label: string;
}
