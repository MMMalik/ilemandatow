import { Meta, Story } from "@storybook/addon-docs/blocks";
import PageTitle from "./PageTitle";

<Meta title="Layout/PageTitle" />

export const Template = (args) => <PageTitle {...args} />;

# Paper

<Story name="Main" args={{ title: "Page Title", subTitle: "Some Subtitle" }}>
  {Template.bind({})}
</Story>
